# Development and CI-only dependencies
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Code formatting / linting
black>=23.0.0
ruff>=0.15.14

# Pre-commit hooks (runs CI checks locally before commit)
pre-commit>=4.0.0

# Security scanning
bandit[toml]>=1.7.0
pip-audit>=2.10.0
safety>=3.7.0
pip-licenses>=5.5.5
cyclonedx-bom>=4.0.0
# trufflehog installed separately in CI workflows

# Documentation
mkdocs>=1.5.0
mkdocs-material>=9.7.6
